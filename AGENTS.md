# AGENTS.md

## Agent skills

### Issue tracker

Los issues viven como ficheros markdown bajo `.scratch/<feature>/` en este repo. See `docs/agents/issue-tracker.md`.

### Triage labels

Los cinco roles canónicos, con el nombre de etiqueta igual al rol. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: un `CONTEXT.md` y `docs/adr/` en la raíz. See `docs/agents/domain.md`.
